# LoopHole

Start with [CONTRACT.md](CONTRACT.md) — the team contract, the go/no-go gates,
folder ownership, and the shared data shapes. Nothing here makes sense without it.

```
pip install -r requirements.txt
```

Set `ANTHROPIC_API_KEY` in your environment (never commit it — see `.gitignore`).
