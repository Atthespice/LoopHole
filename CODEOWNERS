# See CONTRACT.md "Review" section.
# caleb-kylib reviews every PR before it merges to main (5th machine, no folder
# of their own). This auto-requests the review; it does not block a merge —
# GitHub doesn't offer branch protection on free-account private repos, so the
# "wait for approval" rule is by team agreement.
* @caleb-kylib
